import { Link } from "react-router-dom";

interface SidebarProps {
  isCollapsed: boolean;
}

const Sidebar = ({ isCollapsed }: SidebarProps) => {
  return (
    <aside
      className={`fixed md:relative transition-all duration-500 ease-in-out 
      ${
        isCollapsed
          ? "transform -translate-x-full md:translate-x-0 md:w-20 opacity-0 md:opacity-100"
          : "transform translate-x-0 w-[248px]"
      }
      min-h-[789px] bg-gradient-to-b from-gray-800 to-gray-900 text-white shadow-lg`}
    >
      <nav className="flex flex-col justify-between h-full p-[30px_12px] overflow-hidden">
        <ul className="space-y-4">
          <li>
            <Link
              to="/students"
              className="flex items-center px-4 py-2 hover:bg-gray-700 rounded-lg transition-colors"
            >Students</Link>
          </li>
          <li>
     <Link
              to="/students"
              className="flex items-center px-4 py-2 hover:bg-gray-700 rounded-lg transition-colors"
            >Students</Link>
          </li>
        </ul>
      </nav>
    ></aside>